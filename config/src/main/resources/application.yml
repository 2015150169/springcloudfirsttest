
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cwhConfig/config-repo
          username: cwhConfig
          password: 8824623@Whqaz
          #本地仓库
          basedir: D:\JavaProject\IdeaProject\springcloud\config\basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh